<Window x:Class="AutoUploadQCGate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Auto Upload QC Gate"
        Height="720"
        Width="1200"
        MinHeight="560"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#F3F6FA">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDE4EC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1769E0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="36"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1257BD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0D4599"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#B9C5D2"/>
                    <Setter Property="Foreground" Value="#F4F6F8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#EDF2F7"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#CBD5E1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StartButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#1F9D55"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#16833F"/></Trigger>
                <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="#106B32"/></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StopButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#DC3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#C62828"/></Trigger>
                <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="#A51F1F"/></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusTabStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="TabBorder"
                                Background="#F1F5F9"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1"
                                CornerRadius="16"
                                Padding="13,7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#E8EEF7"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#E8F1FF"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#1769E0"/>
                                <Setter Property="Foreground" Value="#1257BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterInputStyle" TargetType="Control">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>

        <Style x:Key="JudgementBadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="8,2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Judgement}" Value="PASS">
                    <Setter Property="Background" Value="#EAF8EF"/>
                    <Setter Property="BorderBrush" Value="#75C68B"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Judgement}" Value="FAIL">
                    <Setter Property="Background" Value="#FDECEC"/>
                    <Setter Property="BorderBrush" Value="#EF8D8D"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="JudgementTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Judgement}" Value="PASS"><Setter Property="Foreground" Value="#16833F"/></DataTrigger>
                <DataTrigger Binding="{Binding Judgement}" Value="FAIL"><Setter Property="Foreground" Value="#C62828"/></DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="#EEF2F6"/>
            <Setter Property="BorderBrush" Value="#C8D1DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="11"/>
            <Setter Property="Padding" Value="9,3"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Processing">
                    <Setter Property="Background" Value="#FFF5DE"/>
                    <Setter Property="BorderBrush" Value="#F3C567"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Success">
                    <Setter Property="Background" Value="#EAF8EF"/>
                    <Setter Property="BorderBrush" Value="#75C68B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Failed">
                    <Setter Property="Background" Value="#FDECEC"/>
                    <Setter Property="BorderBrush" Value="#EF8D8D"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="NeedsReview">
                    <Setter Property="Background" Value="#FFF5DE"/>
                    <Setter Property="BorderBrush" Value="#F3C567"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Processing"><Setter Property="Foreground" Value="#B47300"/></DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Success"><Setter Property="Foreground" Value="#16833F"/></DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Failed"><Setter Property="Foreground" Value="#C62828"/></DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="NeedsReview"><Setter Property="Foreground" Value="#B45309"/></DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="20,16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Auto Upload QC Gate" FontSize="24" FontWeight="Bold" Foreground="#172033"/>
                    <TextBlock Text="Monitor and manage automatic QC Gate uploads" Margin="0,4,0,0" Foreground="#64748B"/>
                </StackPanel>

                <Border x:Name="appStatusBadge" Grid.Column="1" Background="#EEF2F6" CornerRadius="14" Padding="12,6" Margin="0,0,12,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse x:Name="appStatusDot" Width="8" Height="8" Fill="#94A3B8" Margin="0,0,7,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtAppStatus" Text="Stopped" FontWeight="SemiBold" Foreground="#475569"/>
                    </StackPanel>
                </Border>

                <Button Grid.Column="2" Content="Settings" Style="{StaticResource SecondaryButton}" Click="SettingsButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16,12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Automatic upload service" FontWeight="SemiBold" Foreground="#1E293B"/>
                    <TextBlock Text="Start or stop the background scan without changing the current filters." Margin="0,3,0,0" Foreground="#64748B"/>
                    <TextBlock x:Name="txtReuploadWarning"
                               Text="Reupload is blocked until the database migration is applied."
                               Margin="0,5,16,0"
                               Foreground="#B45309"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               Visibility="Collapsed"/>
                </StackPanel>
                <Button x:Name="btnStartScan" Grid.Column="1" Content="Start Auto Upload" Style="{StaticResource StartButton}" Margin="0,0,10,0" Click="btnStartScan_Click"/>
                <Button x:Name="btnStopScan" Grid.Column="2" Content="Stop" Style="{StaticResource StopButton}" Click="btnStopScan_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
                    <RadioButton x:Name="tabAll" GroupName="StatusTabs" Tag="All" IsChecked="True" Style="{StaticResource StatusTabStyle}" Checked="StatusTab_Checked">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="All"/><TextBlock x:Name="txtAllCount" Text="0" Margin="7,0,0,0"/></StackPanel>
                    </RadioButton>
                    <RadioButton GroupName="StatusTabs" Tag="Pending" Style="{StaticResource StatusTabStyle}" Checked="StatusTab_Checked">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="Pending"/><TextBlock x:Name="txtPendingCount" Text="0" Margin="7,0,0,0"/></StackPanel>
                    </RadioButton>
                    <RadioButton GroupName="StatusTabs" Tag="Processing" Style="{StaticResource StatusTabStyle}" Checked="StatusTab_Checked">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="Processing"/><TextBlock x:Name="txtProcessingCount" Text="0" Margin="7,0,0,0"/></StackPanel>
                    </RadioButton>
                    <RadioButton GroupName="StatusTabs" Tag="Success" Style="{StaticResource StatusTabStyle}" Checked="StatusTab_Checked">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="Success"/><TextBlock x:Name="txtSuccessCount" Text="0" Margin="7,0,0,0"/></StackPanel>
                    </RadioButton>
                    <RadioButton GroupName="StatusTabs" Tag="Failed" Style="{StaticResource StatusTabStyle}" Checked="StatusTab_Checked">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="Failed"/><TextBlock x:Name="txtFailedCount" Text="0" Margin="7,0,0,0"/></StackPanel>
                    </RadioButton>
                    <RadioButton GroupName="StatusTabs" Tag="NeedsReview" Style="{StaticResource StatusTabStyle}" Checked="StatusTab_Checked">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="Needs review"/><TextBlock x:Name="txtNeedsReviewCount" Text="0" Margin="7,0,0,0"/></StackPanel>
                    </RadioButton>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" MinWidth="260"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Search" FontSize="11" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtSearch" Style="{StaticResource FilterInputStyle}" TextChanged="SearchTextBox_TextChanged" ToolTip="Search Combine Indication, Customer Code or Log"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Width="155" Margin="0,0,12,0">
                        <TextBlock Text="From date" FontSize="11" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dateFrom" Style="{StaticResource FilterInputStyle}" SelectedDateChanged="DateFilter_SelectedDateChanged"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Width="155" Margin="0,0,12,0">
                        <TextBlock Text="To date" FontSize="11" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dateTo" Style="{StaticResource FilterInputStyle}" SelectedDateChanged="DateFilter_SelectedDateChanged"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" VerticalAlignment="Bottom">
                        <Button Content="Clear filters" Style="{StaticResource SecondaryButton}" Click="ClearFilters_Click"/>
                    </StackPanel>

                    <TextBlock x:Name="txtDateValidation" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="From date must be before To date." Foreground="#C62828" FontSize="11" Margin="0,5,0,0" Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,0,0,12" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F8FAFC" BorderThickness="0,0,0,1" BorderBrush="#DDE4EC" Padding="0,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.15*"/>
                            <ColumnDefinition Width="2.7*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.35*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="COMBINE INDICATION" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="16,11"/>
                        <TextBlock Grid.Column="1" Text="UPLOAD QTY" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="RE-UPLOAD COUNT" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="CUSTOMER" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="LOG" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11"/>
                        <TextBlock Grid.Column="5" Text="JUDGEMENT" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="6" Text="STATUS" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="7" Text="UPLOADED AT" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="12,11"/>
                    </Grid>
                </Border>

                <ItemsControl x:Name="DataList" Grid.Row="1"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer Focusable="False" CanContentScroll="True">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><VirtualizingStackPanel/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#EDF1F5" Padding="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="1.15*"/>
                                        <ColumnDefinition Width="2.7*"/>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="1.35*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="16,9">
                                        <TextBlock Text="{Binding CombineIndication}" Foreground="#1E293B" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding DisplaySubtitle}" Foreground="#94A3B8" FontSize="11" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding UploadQuantity, StringFormat=N0}" Margin="12,9" VerticalAlignment="Center" Foreground="#475569" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding ReuploadRequestCount, StringFormat=N0}" Margin="12,9" VerticalAlignment="Center" Foreground="#475569" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding CustomerName}" Margin="12,9" VerticalAlignment="Center" Foreground="#475569" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding CustomerName}"/>
                                    <Button Grid.Column="4"
                                            Tag="{Binding Log}"
                                            Margin="0"
                                            Padding="12,9"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Center"
                                            Cursor="Hand"
                                            ToolTip="Click to view full log"
                                            Click="LogDetailButton_Click">
                                        <TextBlock Text="{Binding Log}"
                                                   Foreground="#64748B"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="54"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Button>
                                    <Border Grid.Column="5" Margin="10,7" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource JudgementBadgeStyle}">
                                        <TextBlock Text="{Binding Judgement}" Style="{StaticResource JudgementTextStyle}"/>
                                    </Border>
                                    <Border Grid.Column="6" Margin="10,7" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource StatusBadgeStyle}">
                                        <TextBlock Text="{Binding Status}" Style="{StaticResource StatusTextStyle}"/>
                                    </Border>
                                    <TextBlock Grid.Column="7" Text="{Binding UploadedAt}" Margin="12,9" VerticalAlignment="Center" Foreground="#64748B"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel x:Name="emptyState" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" IsHitTestVisible="False">
                    <TextBlock Text="No upload records found" FontSize="17" FontWeight="SemiBold" Foreground="#475569" HorizontalAlignment="Center"/>
                    <TextBlock Text="Try changing or clearing the current filters." Margin="0,5,0,0" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource CardStyle}" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Total records" Foreground="#64748B" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtTotalRecords" Text="0" FontWeight="Bold" FontSize="15" Foreground="#1E293B" Margin="7,0,24,0" VerticalAlignment="Center"/>
                <TextBlock Text="Successful" Foreground="#64748B" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtOKRecords" Text="0" FontWeight="Bold" FontSize="15" Foreground="#16833F" Margin="7,0,24,0" VerticalAlignment="Center"/>
                <TextBlock Text="Failed" Foreground="#64748B" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtNGRecords" Text="0" FontWeight="Bold" FontSize="15" Foreground="#C62828" Margin="7,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid x:Name="logDetailOverlay" Grid.RowSpan="5" Background="#800F172A" Panel.ZIndex="100" Visibility="Collapsed">
            <Border Width="760" Height="470" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="10" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Background="#1769E0" CornerRadius="10,10,0,0" Padding="20,14">
                        <Grid>
                            <TextBlock Text="LOG DETAILS" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Content="×" HorizontalAlignment="Right" Width="32" Height="28" Padding="0"
                                    Background="Transparent" BorderThickness="0" Foreground="White" FontSize="22"
                                    Cursor="Hand" Click="CloseLogDetail_Click" ToolTip="Close"/>
                        </Grid>
                    </Border>

                    <TextBox x:Name="txtLogDetail" Grid.Row="1" Margin="20" Padding="12"
                             IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                             Background="#F8FAFC" BorderBrush="#CBD5E1" FontFamily="Consolas" FontSize="13"/>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,18">
                        <Button Content="Close" Style="{StaticResource SecondaryButton}" Click="CloseLogDetail_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
